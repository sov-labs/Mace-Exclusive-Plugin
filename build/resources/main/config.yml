settings:
  # Language: en or vi
  language: en
  # Can the mace be dropped by the player?
  allow-drop: true
  # Tracking mode: simplified (true) or strict (false)
  # Strict mode prevents moving to other inventories etc. if implemented similar to original
  strict-mode: false

mace:
  name: "&b&lMACE OF POWER"
  material: MACE
  custom-model-data: 2001
  lore:
    - "&7An ancient weapon."
    - "&7Hold to charge power."
    - "&7&oOnly one can exist."
  
  # Recipe for the Power Mace
  recipe:
    enabled: true
    shape:
      - " H "
      - " I "
      - " B "
    ingredients:
      H: HEAVY_CORE
      I: NETHERITE_INGOT
      B: BREEZE_ROD
